version: '3.8'

services:
  web:
    image: ${DOCKERHUB_USERNAME}/my-django-app:latest
    container_name: django-prod-app
    restart: always

    ports:
      - "8000:8000"
    env_file:
      - .env.prod